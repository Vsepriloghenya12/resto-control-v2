[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install pg@8.21.0 --no-save --omit=dev --no-audit --no-fund --ignore-scripts --no-package-lock"]

[phases.build]
cmds = ["npm run check", "test -f dist/index.html"]

[start]
cmd = "npm run start"
