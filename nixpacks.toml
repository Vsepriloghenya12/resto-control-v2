[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install --omit=dev --no-audit --no-fund"]

[phases.build]
cmds = ["npm run check", "test -f dist/index.html"]

[start]
cmd = "npm run start"
