[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["echo Runtime node_modules is included; skipping npm install"]

[phases.build]
cmds = ["npm run check && test -f dist/index.html"]

[start]
cmd = "npm run start"
